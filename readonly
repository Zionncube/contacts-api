## ✅ 1. Qala i-server yakho

Qinisekisa ukuthi usebenzisa i-command:

```bash
node server.js
```

Uma yonke into ihamba kahle uzobona:

```
✅ Connected to MongoDB
🚀 Server running on port 3000
```

---

## ✅ 2. Hlola i-endpoints ku-Thunder Client

### ➡️ (a) **GET all contacts**

* Method: `GET`
* URL: `http://localhost:3000/api/contacts`
* Kulindeleke ukuthi uphinde uthole i-JSON yawo wonke ama-contacts akhona ku-DB yakho.

---

### ➡️ (b) **POST create contact**

* Method: `POST`
* URL: `http://localhost:3000/api/contacts`
* Body → khetha **JSON** bese ufaka:

```json
{
  "name": "Happiness Ncube",
  "email": "happiness@gmail.com",
  "phone": "0678731152"
}
```

* Uma kuyaphumelela, uzothola i-response enento oyifakile, kanye ne-`_id`.

---

### ➡️ (c) **GET one contact by ID**

* Method: `GET`
* URL: `http://localhost:3000/api/contacts/<ID>`
  (thatha i-`_id` oyitholile ku-POST step).

---

### ➡️ (d) **PUT update contact**

* Method: `PUT`
* URL: `http://localhost:3000/api/contacts/<ID>`
* Body:

```json
{
  "name": "Happiness Updated",
  "email": "happiness_updated@gmail.com",
  "phone": "0123456789"
}
```

---

### ➡️ (e) **DELETE contact**

* Method: `DELETE`
* URL: `http://localhost:3000/api/contacts/<ID>`
* Uzothola impendulo:

```json
{ "message": "Contact deleted" }
```

---

## 💡 Tips

* Uma usudinga ukuyihambisa ku-**Render**, sebenzisa `https://<your-app-name>.onrender.com/api/contacts` esikhundleni se-`http://localhost:3000/api/contacts`.
* Qinisekisa ukuthi `MONGO_URI` yakho ku-`.env` isethwe kahle.

---

Ngabe ufuna ngikwenzele **screenshot-style walkthrough** (njengokuthi ungachofoza kuphi ku-Thunder Client ku-VS Code) ukuze kube lula uku-follow?

MONGO_URI=mongodb+srv://mercymsawenkosi_db_user:Zionzion@cluster0.qoiu33r.mongodb.net/?retryWrites=true&w=majority

contacts-api/
├── controllers/
│   └── contacts.js      # logic for CRUD operations
├── routes/
│   └── contacts.js      # defines endpoints (GET, POST, PUT, DELETE)
├── db/
│   └── connect.js       # MongoDB connection file
├── swagger.js           # swagger setup
├── server.js            # main entry point
├── package.json
├── .env
└── .rest                # rest client tests


Deployment to Render

Push code to GitHub.

Go to Render → New Web Service → connect repo.

Add environment variable MONGODB_URI in Render settings.

Render will give you a URL like:

